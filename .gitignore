# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Updater signing key — NEVER commit the private key. Public key value is copied
# into tauri.conf.json instead; keep .keys/ only on the machine that generated it
# and in the GitHub Actions secret (TAURI_SIGNING_PRIVATE_KEY).
.keys/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
